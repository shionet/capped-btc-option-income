from app.domain.models import SpreadCandidate


def build_iron_condors(*args, **kwargs) -> list[SpreadCandidate]:
    # TODO phase 2:
    # combine one bull put spread and one bear call spread with same expiry.
    return []
